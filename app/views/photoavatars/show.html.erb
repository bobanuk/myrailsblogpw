<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @photoavatar.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @photoavatar.content %>
</p>

<%= link_to 'Edit', edit_photoavatar_path(@photoavatar) %> |
<%= link_to 'Back', photoavatars_path %>
